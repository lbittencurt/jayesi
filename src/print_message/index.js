/**
 * This function is responsible call console.log to print
 * a text it into the console from a variable in the function params.
 */
export default function printMessage(msg = 'Hello again.') {
  //TODO: type here your code
}